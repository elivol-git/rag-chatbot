flask==3.1.0
flask-cors==5.0.1
python-dotenv==1.0.1
numpy==2.2.3
requests==2.32.3
pypdf==5.3.0
python-docx==1.1.2
python-pptx==1.0.2
mcp==1.4.1
pytest==8.3.4
# WSGI server for the container. Marked off on Windows: gunicorn's worker model
# is POSIX-only, and the local dev flow there is `python -m src.app`.
gunicorn==23.0.0; sys_platform != "win32"
